[project]
name = "swm-sdk"
version = "0.1.0"
description = "Software Web Manager Python SDK"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["requests>=2.31.0"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
